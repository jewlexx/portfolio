---
import { twMerge } from "tailwind-merge";

const { class: className, ...props } = Astro.props;
---

<a
  {...props}
  class={twMerge(
    "btn btn-ghost lg:btn-block lg:text-2xl hover:font-bold text-xl",
    className,
  )}
  data-astro-prefetch="hover"
>
  <slot />
</a>
