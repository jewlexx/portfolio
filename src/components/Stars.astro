---
import stars from 'stars';

const canvas = document.getElementById("stars");

if (canvas === null) {
    console.error("Could not find stars background canvas");
} else {
    stars.attach(canvas as HTMLCanvasElement);
}

---

<canvas id="stars"></canvas>