---
import Layout from '../layouts/Project.astro';
import { getEntry } from 'astro:content';

const about = await getEntry('info', 'about');

const { Content } = await about.render();
---

<Layout title={about.data.title} description="A bit about Juliette Cordor" slug={about.data.title}>
	<Content />
</Layout>
